{
  "name": "meow-conomy",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "node src/game.test.mjs",
    "test:watch": "node --watch src/game.test.mjs",
    "check": "npm test",
    "stamp": "node scripts/stamp-version.mjs",
    "serve": "sh -c 'python3 -m http.server ${PORT:-5173} --directory site'",
    "start": "npm run serve",
    "dev": "npm run serve"
  }
}
